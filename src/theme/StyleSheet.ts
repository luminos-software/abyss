import { ImageStyle, TextStyle, ViewStyle } from "react-native";

export const viewStyle = (input: ViewStyle): ViewStyle => input;
export const textStyle = (input: TextStyle): TextStyle => input;
export const imageStyle = (input: ImageStyle): ImageStyle => input;